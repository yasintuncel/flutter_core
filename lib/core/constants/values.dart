import 'package:flutter/material.dart';

const double defaultPaddingValue = 8.0;
const double defaultRadiusValue = 8.0;

const EdgeInsets defaultPadding = EdgeInsets.all(defaultPaddingValue);
const BorderRadius defaultRadius = BorderRadius.all(Radius.circular(defaultRadiusValue));
